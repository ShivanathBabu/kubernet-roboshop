apiVersion: v1
kind: Service
metadata:
  name: mongodb
  labels:
    app: database
spec:
  clusterIP: None
  selector:
    app: database
  ports:
  - protocol: TCP
    port: 27017
    targetport: 27017